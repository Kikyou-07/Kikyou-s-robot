# Security policy

Please do not open a public issue containing an API key, chat export, database,
attachment, or other private material. Report a suspected vulnerability through
the repository owner's private contact channel instead.

If a credential is ever pushed to a public repository, revoke or rotate it
immediately. Removing the file in a later commit is not sufficient because the
old commit remains in Git history.
